import 'package:flutter/material.dart';

class ProjectExecutorPage extends StatelessWidget {
  const ProjectExecutorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Project Executor')),
      body: Center(child: Text('Welcome to the Project Executor Page')),
    );
  }
}
