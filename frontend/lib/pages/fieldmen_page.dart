import 'package:flutter/material.dart';

class FieldMenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Field Men')),
      body: Center(child: Text('Welcome to the Field Men Page')),
    );
  }
}
